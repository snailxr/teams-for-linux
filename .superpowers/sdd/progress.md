feat/conversation-summary — Conversation Summary & Draft Reply
Task 1: complete (app/_claudeRunner.js created, app/polishInput/index.js refactored; 325 tests pass, lint clean; NOT committed). Note: repo ESLint enforces preserve-caught-error -> re-throws in catch need { cause: err }.
Task 2: complete (app/browser/tools/_composeReplace.js created, polishInput.js refactored 243->193 lines, methods removed; 325 tests pass, lint clean; NOT committed).
Task 3: complete (app/conversationSummary/index.js + tests/unit/conversationSummary.test.js created; buildPrompt with { cause: err } fix; 328 tests pass, lint clean; NOT committed).
Task 4: complete (app/browser/tools/conversationSummary.js created + 4 extractMessages tests; 332 tests pass, lint clean; NOT committed). DEVIATION (justified): extractMessages falls back to raw innerText ONLY when neither body nor author selector matches (plan code failed the "drops empties" test). Trade-off: an item with recognized author but unrecognized body is dropped -> Task 7 selector verification covers this.
Task 5: complete (preload.js module+ipc set, index.js require+register, ipcValidator allowlist; trayIconRenderer/mqttStatusMonitor preserved; modules load OK, 332 tests pass, lint clean; NOT committed).
Task 6: complete (CUSTOM_FEATURES.md entry; done inline by controller).
Task 7: MANUAL — live-DOM selector verification, requires user running npm start + logged-in Teams. NOT done.
Final review (opus): 2 Important fixed by controller -> (1) popup now renders htmlToPlain via textContent (no innerHTML; closes untrusted-message injection) + dead ul/ol CSS removed; (2) extractMessages falls back to author-stripped innerText when body selector misses (was silently dropping messages) + regression test added. 333 tests pass, lint clean. 3 Minor deferred: detached-button busy-restore (harmless), htmlToPlain<code> (no bug), reply overwrites steering text (by design). NOT committed. Task 7 (live-DOM selectors) still MANUAL.
